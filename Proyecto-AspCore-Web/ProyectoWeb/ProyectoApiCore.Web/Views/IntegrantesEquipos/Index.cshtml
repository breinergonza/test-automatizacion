@model IEnumerable<ProyectoApiCore.Web.IntegrantesEquipo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idIntegrante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idEquipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idIntegrante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idEquipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idIntegranteEquipo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idIntegranteEquipo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idIntegranteEquipo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
